acceptance_criterion ac_step02_semantic_regions_valid {
  name "Step 02 Semantic Regions Valid"
  description "Given the proof repo is at Step 02, when npm run verify is executed, then reusable region contracts validate, the region catalog artifact lists the semantic work areas, and the proof remains ready for layout composition."
  requirement req_operations_ui_work_map_proof
  status approved
}
