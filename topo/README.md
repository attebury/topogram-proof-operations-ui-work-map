# Operations UI Work Map

> The canonical Topogram workspace for the operations-dashboard UI proof.

Status: current

Audience: agents and reviewers inspecting the proof graph.

Use when: you need the product map behind the proof repo.

Step 01 intentionally includes only the domain shell. Reusable regions, layouts, screens, widgets, design contracts, realization sets, i18n, and ARIA are added in later proof checkpoints.
