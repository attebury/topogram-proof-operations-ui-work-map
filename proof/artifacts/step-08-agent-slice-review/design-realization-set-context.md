# Step 08 Design Realization Context: realization_set_operations_control_room_widgets

> Review card derived from current Topogram slice/report outputs for realization_set_operations_control_room_widgets.

Status: current

Audience: agents and reviewers checking whether focused UI work-map context is usable.

Use when: direct design_realization_set slice selection is not yet available and an agent needs the current fallback context.

## Summary

Maps operations semantic widgets to platform component refs across web, iOS, and Android.

## Source Artifacts

- ui-design-coverage.json
- projection-slice.json
- widget-slice.json

## Current Limitation

Design realization data is visible through coverage/report/widget slices, but there is no direct realization-set selector.

## Verdict

Useful enough as a fallback, but not good enough as the final product shape.
